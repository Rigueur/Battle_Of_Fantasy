<!DOCTYPE html>
<html>
  <head>
    <title>Battle Of Fantasy - Web Browser Fantasy Game</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"
    />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/ionicons/2.0.1/css/ionicons.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag %>
  </head>

  <body  style="height: 100vh; width: 100vw; overflow: hidden; background-image: url('<%= asset_path (current_user ? current_user.towns.first.image_url : 'desert-base-1.png')%>'); background-size: cover;">
    <div class="page-container" style="width: 100vw; height:100vh; position: relative;">
      <%= yield %>
      <%= render "shared/footer" %>
      <%= render "shared/flashes" %>
    </div>
  </body>
</html>
